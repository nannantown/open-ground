# OPEN GROUND コンセプト

## 一言で

**N枚のターミナルを1枚のキャンバスに畳む — Claude Code のコックピット。**

複数プロジェクトに Claude Code を走らせる開発者のためのローカルツール。
全プロジェクトを無限キャンバスにカードで並べ、どのカードからでも Claude を
起動・監視できる。プロダクトマネジメントは「全部を一望して動かせる場所が
できた」**副産物**として生まれる。

---

## 前提 — パワーと UI のねじれ

AI の使い方には2つの世界がある。

- **ウェブの AI**（Claude.ai / ChatGPT）— UI はある。だが複数プロジェクトを
  同時に回す前提で作られていない。本格的に使うほど視認性が破綻する。
- **ターミナルの AI エージェント**（Claude Code / Codex などサブスクで動く CLI）—
  **最も強力**。だが UI が無い。ターミナルゆえに使いにくい。

ねじれている ── **一番パワーがある道具が、一番 UI が貧弱**。そして誰も、これを
「複数プロジェクトを並行で回す」用途に橋渡ししていない。

**OPEN GROUND はその橋。** ターミナル AI エージェントのパワーに、ポートフォリオ規模の
UI を、ローカルで与える。「AI と UI を連結する」とはこの橋のこと。

下の「倒したい敵」は、このねじれの*症状*。OPEN GROUND が日々直すのは症状だが、
狙う根は常にこのねじれにある。

---

## 倒したい敵

毎回プロジェクトのパスに `cd` して `claude` を起動する。それを何個もやる。
ターミナルのウィンドウ／タブが何枚も開く。そのタブ・ウィンドウの切り替えで
頭のリソースが溶ける。

OPEN GROUND はこの **「起動の儀式」と「ウィンドウだらけ」** を消す。

腐ったプロジェクトは敵ではない（捨てればいい）。敵は、**健在なプロジェクトを
動かすときの摩擦**。

---

## 性格 — マルチプレクサ

OPEN GROUND は「同じ指示を全プロジェクトに一斉発射するバッチツール」ではない。

各カードが**独立した別々の作業**を持つ **マルチプレクサ**。N個の異なる
Claude の仕事を1画面に束ねる。共通テンプレートの一斉発射は、その**特殊ケース**に
すぎない。

---

## 2層構造

### 層1 — 操作盤（OPEN GROUND の顔）

- 全プロジェクトフォルダを1枚の無限キャンバスにカード表示
- コア体験は **俯瞰**
- 位置は**自由な作業場** — システムは位置に意味を読まない。だから層1は
  「地図」というより **操作盤**。位置が語らないぶん、**カードの状態表示が
  俯瞰のすべてを背負う**
- フレームでカードを束ねられる（あくまでユーザーの整理用）
- **今フォーカスするのはこの層。ノードはプロジェクトのみ。**

### 層2 — プロジェクト内キャンバス（実装中）

カードをドリルダウンすると、そのプロジェクト専用のワークスペース。
4 つのタブで構成される:

- **Chats** — タスクごとの会話スレッド（旧 Tasks。ChatGPT ライクなサイドバー）
- **Terminal** — プロジェクト内対話シェル（複数スロット並列）
- **Canvas** — Chrome 風タブで複数 Canvas を切替可能なデザイン／ブレスト面。
  Sticky / Text / Frame / Mock（live React/HTML iframe）/ Comment を配置でき、
  各 Canvas は独自のチャットサイドバーを持つ。
- **Overview** — メタ情報ダッシュボード

各 Canvas は `<project>/.openground/canvases/<id>.json` に独立保存される。
1 プロジェクト = 複数 Canvas の束。

---

## カード — 操作盤の主役

- **カードの主役（一番大きく出る情報）= 直近の Claude 実行の結果サマリ。**
  各カードが「このプロジェクトは今こうだ」を1〜2行で語る。
  キャンバス＝AIワーカーたちからの短い近況報告の壁。
- カードへの指示（プロンプト）は **2系統**：`tasks.json` のタスクリストから選ぶ /
  その場でアドホックに打ち込む。両方使える。
- 個別にも一斉にも撃てる。

### 実行状態 — 4つ

撃った後、カードが今どの段階にいるかが一目で分かること。

```
[アイドル/未実行] ──撃つ──> 待機中 ──枠が空く──> 実行中 ──終了──> 完了
       ▲                                                          │
       └──────────────── 次を撃つまで「完了」のまま ─────────────┘
```

- **アイドル／未実行** — 一度も撃っていない静止状態。説明文か「未実行」を出す。
- **待機中** — 撃ったがワーカー枠の空き待ち（同時実行数を絞るのでキューは必然）。
- **実行中** — Claude が動いている。経過時間や最後のログ行で「生きている」ことを見せる。
- **完了** — 終わった。**フラットに1状態**（クリーン / ブロッカー / 失敗で割らない）。
  次を撃つまでこのまま居座る。

状態バッジは「どこまで進んだか（段階）」だけを担う。「うまくいったか（良し悪し）」は
主役の結果サマリ本文が担う。完了を結果で割らないぶん、**サマリ本文が「このカードは
要対応か」を伝える唯一の手段**になる ＝ サマリ品質が製品の生命線、の含意がここでも効く。

---

## AI との関わり — 投げて待つ＋セッション resume

基本は「投げて待つ」（PTY 上の `claude --dangerously-skip-permissions` 起動
／結果サマリを観測）。

ただし以下のケースでは対話を継続できる:

- **Resume**: 同じタスク／Canvas chat に追加で発言すると、Claude の同 session を
  `--resume` で継続する。ユーザーが介入するための合意的な「次の発言」。
- **Auto-loop**: `taskComplete: false` で終わったラウンドは、最大 `AUTO_MAX_ROUNDS`
  回まで自動的に「続きを進めてください」プロンプトで resume する。ユーザーが
  予約送信キューを持っている、または composer に focus がある間は自走しない。
- **Question turn**: Claude が `OPENGROUND_RESULT.question` 付きで返した時は、
  auto-loop は一旦停止し、ユーザーの返事を待つ。

長期対話ではないが「ワンショットの厳格な単発」でもない。**resume を許容し、
ユーザーが入る隙を残す前提で動く。**

---

## 設計上の含意

1. **ランナーの再定義** — 「1テンプレート→Nプロジェクト」から
   「N枚のカード・各自のプロンプト・個別にも一斉にも発射」へ。
   `{{tasks}}` / `{{notes}}` の個別化は土台。足りないのは
   「カードごとに違う指示を保持できる」こと。
2. **サマリ品質が製品の生命線** — カードの主役が結果サマリである以上、
   `PMMAP_RESULT` のサマリは「何をしたか」でなく「このプロジェクトは
   今どこにいるか」を語らせる必要がある。`RESULT_INSTRUCTION` が最重要サーフェス。
3. **未実行カードのフォールバック** — サマリがまだ無いカードは、
   プロジェクト説明文や「未実行」を主役にする。
4. **データモデルの拡張余地** — 層2（プロジェクト＝複数キャンバスの束）を
   命名・構造で塞がない。
5. **情報密度** — 「俯瞰」を裏切らない。サマリが一目で読めることを装飾より優先。
6. **エージェント差し替えの抽象** — runner は `claude` 決め打ちでなく
   「エージェント CLI をディレクトリ内で prompt 付き起動する」抽象にする。
   当面は Claude Code 一択だが、将来の Codex 等を塞がない。`PMMAP_RESULT` 規約は
   エージェント非依存（プロンプトで指示すればどのエージェントでも出せる）。

---

## スコープ宣言

| | 内容 |
|---|---|
| **今** | 層 1（コックピット）＋ 層 2（Chats + Terminal + Canvas + Overview）＋ resume／auto-loop 付きの実行 |
| **次** | Canvas に live React module を貼る（ScreenElement）／ 複数エージェント対応 |
| **変えない** | ローカル単一ユーザー、ファイルシステム母艦 |
